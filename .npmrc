# Enable workspaces feature
workspaces=true

# Use workspace protocol for local dependencies
link-workspace-packages=true

# Ensure packages can access each other during development
shared-workspace-lockfile=true